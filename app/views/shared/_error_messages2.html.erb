<% if @car_info.errors.any? %>
<div id="error_explanation">
<h2><%= pluralize(@car_info.errors.count, "error") %>

<p>There were problems with the following fields:</p>
<ul>
<% @car_info.errors.full_messages.each do |msg| %>
<li><%= msg %></li>
<% end %>
</ul>
</div>
<% end %>
